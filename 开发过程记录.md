# 开发过程记录

本项目采用逐步开发的方式完成，以下是各阶段的开发内容：

## 阶段 1：数据结构设计与数据读取
- 设计 `DataDef.h`：图结构（邻接表）、包裹结构体、节点信息
- 实现 `loadData()` 读取 map.txt / packages.txt / car.txt
- 支持中文路径（wmain + 宽字符）
- **提交信息**: "feat: 初始化数据结构与文件读取模块"

## 阶段 2：最小堆与 Dijkstra 最短路 (T1)
- 手写 `MinHeap` 二叉堆（不使用 std::priority_queue）
- 实现 Dijkstra 单源最短路算法
- 添加最短路缓存（sp_cache）减少重复计算
- 路径回溯与输出
- **提交信息**: "feat: 实现手写最小堆与Dijkstra最短路算法(T1)"

## 阶段 3：循环队列与 T2 不满意度最小化 (T2)
- 实现 `CircularQueue` 模板循环队列
- 实现包裹到站→入队→装车流水线
- T2 配送模拟：考虑到站时间、容量限制
- 基线策略：按到站时间排序
- 改进策略：综合等待时间+距离评分
- **提交信息**: "feat: 实现循环队列与T2不满意度最小化配送"

## 阶段 4：T3 带容量的运送成本计算 (T3)
- 逐段成本计算：段长 × (自重 + 当前载重)
- 超时判断：送达时间 > 最晚送达时间
- 基线策略：按截止时间排序
- 改进策略：综合截止时间+距离评分
- **提交信息**: "feat: 实现T3带容量约束的运送成本计算"

## 阶段 5：T4/T5/T6 选做任务
- T4：状态压缩DP（≤15点）+ 最近邻启发式（>15点）
- T5：多候选分车 + 局部交换/搬移优化
- T6：三种场景（原始/等待压力/截止压力）× 两种策略
- **提交信息**: "feat: 完成T4退货回收、T5双车协同、T6策略对比"

## 阶段 6：可视化界面
- 生成 visualization_data.js（JSON 格式的完整结果数据）
- 创建 visualization.html：Canvas 交互式地图
- 支持 T1-T6 全部视图切换
- 配送动画播放功能
- 鼠标悬停节点信息
- **提交信息**: "feat: 添加完整可视化界面与JSON数据导出"

## 阶段 7：项目报告与文档
- 更新项目报告.md（含复杂度分析、测试结果）
- AI_USAGE.md 使用日志
- 开发过程记录（本文件）
- **提交信息**: "docs: 更新项目报告与开发文档"

---

如需重建 Git 历史，在项目目录运行：
```bash
git init
git add DataDef.h MinHeap.h MinHeap.cpp CircularQueue.h
git commit -m "feat: 初始化数据结构与文件读取模块"

git add Solver.h Solver.cpp main.cpp
git commit -m "feat: 实现手写最小堆与Dijkstra最短路算法(T1)"

git add CircularQueue.h Solver.cpp
git commit -m "feat: 实现循环队列与T2不满意度最小化配送"

git add Solver.cpp
git commit -m "feat: 实现T3带容量约束的运送成本计算"

git add Solver.cpp main.cpp
git commit -m "feat: 完成T4退货回收、T5双车协同、T6策略对比"

git add visualization.html Solver.cpp main.cpp
git commit -m "feat: 添加完整可视化界面与JSON数据导出"

git add README.md AI_USAGE.md 项目报告.md 开发过程记录.md
git commit -m "docs: 更新项目报告与开发文档"
```
