# 菜鸟驿站配送调度系统项目报告

## 1. 项目概述

本项目实现了一个面向校园地图的菜鸟驿站配送调度系统，支持以下全部任务：

- **T1**：单源最短路查询与路径输出（Dijkstra + 手写最小堆）
- **T2**：考虑到站时间的配送，不满意度之和最小化（基线+改进策略对比）
- **T3**：考虑容量、成本与超时数的配送（基线+改进策略对比）
- **T4**：退货回收路线规划（状态压缩 DP / 最近邻启发式）
- **T5**：双车协同配送（多候选分车 + 局部优化）
- **T6**：策略对比实验（3 场景 × 2 策略）
- **可视化**：交互式 HTML Canvas 地图与配送动画

程序使用 C++17 实现，可直接读取题目规定格式的数据目录，支持中文路径，输出结果到控制台并导出 JSON 数据供可视化使用。

---

## 2. 数据结构设计

### 2.1 图结构

地图采用邻接表表示：`vector<vector<Edge>> graph`。

- 每个节点保存若干条出边，适合稀疏图。
- 地图保证连通，边权为非负实数，适合 Dijkstra。
- 邻接表空间复杂度：$O(n+m)$，遍历邻边的效率高于邻接矩阵。

**选用理由**：校园地图节点数通常在几十到几百，边数与节点数同量级（稀疏图），邻接表比邻接矩阵（$O(n^2)$）更节省空间且遍历更快。

### 2.2 最小堆（MinHeap）

Dijkstra 所用优先队列**不使用 `std::priority_queue`**，而是自实现的二叉最小堆：

- 底层用 `vector<HeapNode>` 存储
- `push`: 尾部插入 + 上滤（$O(\log n)$）
- `pop`: 堆顶移除 + 下滤（$O(\log n)$）
- `empty`: $O(1)$

### 2.3 循环队列（CircularQueue）

**新增数据结构**：模板循环队列，用于维护驿站待配送包裹的流水线。

```
包裹到站(S_i) → enqueue(入队) → 装车时 dequeue(出队)
```

- 底层用固定容量数组 + head/tail 指针
- `enqueue`: $O(1)$，自动回绕
- `dequeue`: $O(1)$
- 支持 `empty()`、`full()`、`size()` 查询

**使用场景**：在 T2/T3 模拟中，包裹到站后进入循环队列等待装车；每次规划趟次时从队列中取出可用的包裹。

### 2.4 包裹与车辆数据

- `Package` 结构体：id、weight、dest、arrive、deadline
- `NodeInfo` 结构体：id、x、y、is_station（存储节点坐标用于可视化）
- `car.txt` 读入：speed、car_weight、capacity

### 2.5 最短路缓存

为减少重复 Dijkstra 开销，对每个源点建立缓存：

- `unordered_map<int, ShortestPathCache> sp_cache`
- 缓存从某源点到所有点的最短距离和前驱数组
- 同源点重复查询直接复用，避免重复跑 Dijkstra

---

## 3. 核心算法设计

### 3.1 T1：Dijkstra 单源最短路

对任意源点 `src` 运行 Dijkstra：

1. 初始化距离为 ∞，源点距离为 0
2. 使用自实现 MinHeap 维护 `(dist, node)` 对
3. 每次取堆顶（最小距离节点），松弛其所有邻边
4. 前驱数组记录最优转移，用于回溯恢复路径

支持两种查询模式：
- 默认输出驿站 `0` 到所有节点的最短路
- 命令行指定 `src dst` 时输出任意两点间的最短路

**时间复杂度**：单次 Dijkstra $O((n+m) \log n)$，含缓存后重复查询降至 $O(1)$（路径回溯 $O(n)$）。

### 3.2 T2：最小化不满意度之和

**定义**：不满意度 = $\sum (D_i - S_i)$，包裹只能在到站时间 $S_i$ 之后派送。

**算法流程**：
1. 循环队列收集已到站包裹
2. 按策略优先级排序候选包裹
3. 在容量限制内装载
4. 按最近优先顺序依次送达
5. 返回驿站，时间推进，重复直到全部送达

**基线策略**：按到站时间 $S_i$ 排序，同时间按距离近优先。

**改进策略**：综合考虑距离与等待时间，评分函数 = `distance - 0.35 × wait_time`，优先送距离近且已等待久的包裹。

**正确性**：确保所有包裹在到站后送达，每趟不超载，方案可复现。

### 3.3 T3：运送成本与超时数

**定义**：
- 成本 = $\sum_{每段路}$ 段长 × (自重 + 该段车上包裹总重)
- 超时：$D_i > T_i$

**算法流程**：与 T2 类似但所有包裹 $S_i = 0$，成本逐段累加，逐包判断超时。

**基线策略**：按截止时间 $T_i$ 排序。

**改进策略**：综合截止时间与距离，评分 = `deadline - 0.35 × distance_to_station`，在截止时间接近的前提下倾向于近距离包裹。

**正确性**：每段成本计算包括当前车上全部包裹重量，卸货后重量递减，空车返回只计自重，与题意完全一致。

### 3.4 T4：退货回收

**输入**：`returns.txt`（存在时直接读取，不存在时从包裹目的地推导）。

**算法**：
- 退货点数 ≤ 15：**状态压缩 DP** 求精确最优解，$O(2^r \cdot r^2)$
- 退货点数 > 15：**最近邻贪心启发式**，$O(r^2)$

状态压缩 DP 枚举所有访问排列，保证点数较少时得到全局最优。最近邻启发式在点数多时保证效率和可行。

### 3.5 T5：双车协同

**算法流程**：
1. 4 种初始分配方案（按 deadline / 距离×重量 / 重量 / 综合评分）
2. 每种方案用贪心分车（均衡两车负荷）
3. 局部优化：两车包裹交换 + 单包裹搬移
4. 所有候选方案取最优

**优化停止条件**：连续无改进或超过 12 轮迭代。

### 3.6 T6：策略对比实验

构造 3 组实验场景：

| 场景 | 说明 |
|------|------|
| 原始数据 | 直接使用读入的包裹数据 |
| 等待压力场景 | 包裹到站时间错开（0/2/4/6...），deadline 设为无穷大 |
| 截止时间压力场景 | deadline 设为 `最短距离/速度 + 6 + 偏移`，制造紧迫感 |

每组场景对比基线策略 vs 改进策略，输出 T2 不满意度、T3 成本、T3 超时数。

### 3.7 结果可视化

自建交互式 HTML 可视化界面（`visualization.html`），功能包括：

- **Canvas 地图渲染**：节点、边权重、驿站高亮
- **T1-T6 七种视图切换**：地图全览 / 最短路 / 配送路线 / 成本 / 退货 / 双车 / 策略对比
- **动画播放**：行程逐段播放，速度自适应
- **鼠标交互**：悬停显示节点详情（坐标、距驿站距离）
- **底部数据面板**：Tab 切换显示 T1-T6 详细表格
- **双策略对比**：T2/T3 同时展示基线和改进方案

C++ 程序运行后自动生成 `visualization_data.js`，HTML 加载后即可查看全部结果。

---

## 4. 复杂度分析

| 组件 | 时间复杂度 | 空间复杂度 |
|------|-----------|-----------|
| 邻接表 | - | $O(n + m)$ |
| Dijkstra（单次） | $O((n+m) \log n)$ | $O(n)$ |
| MinHeap::push | $O(\log n)$ | $O(1)$ |
| MinHeap::pop | $O(\log n)$ | $O(1)$ |
| CircularQueue::enqueue | $O(1)$ | $O(1)$ |
| CircularQueue::dequeue | $O(1)$ | $O(1)$ |
| T2/T3 单趟排序 | $O(k \log k)$ | $O(k)$ |
| T2/T3 总体 | $O(趟数 × (k \log k + k \cdot Dijkstra))$ | $O(n + k)$ |
| T4 DP（精确） | $O(2^r \cdot r^2)$ | $O(2^r \cdot r)$ |
| T4 贪心（启发式） | $O(r^2)$ | $O(r)$ |
| T5 双车优化 | $O(轮数 × k^2 × Dijkstra)$ | $O(k)$ |

---

## 5. 测试结果

### 5.1 示例数据验证

对照《测试数据/示例/预期结果.md》自检：

| 检查项 | 预期 | 实际 | 状态 |
|--------|------|------|------|
| 0→3 最短距离 | 18 | 18 | ✅ |
| 0→3 路径 | 0→1→3 | 0→1→3 | ✅ |
| 0→4 最短距离 | 25 | 25 | ✅ |
| 0→4 路径 | 0→1→3→4 | 0→1→3→4 | ✅ |
| 0→5 最短距离 | 24 | 24 | ✅ |
| 0→5 路径 | 0→1→3→5 | 0→1→3→5 | ✅ |
| 每趟载重 ≤20 | 必须 | 满足 | ✅ |
| 最少 2 趟 | 必须 | 2 趟 | ✅ |

### 5.2 示例数据结果汇总

| 任务 | 基线策略 | 改进策略 | 改进幅度 |
|------|----------|----------|----------|
| T2 不满意度之和 | 37.2 | 37.2 | 0% |
| T3 总成本 | 2555 | 2501 | **2.1%** |
| T3 超时数 | 0 | 0 | 0 |
| T4 退货总距离 | 54 | - | - |
| T5 双车总成本 | 2465 | - | - |
| T5 超时数 | 0 | - | - |

### 5.3 T6 策略对比（示例数据）

| 场景 | T2基线 | T2改进 | T3基线 | T3改进 | 基线超时 | 改进超时 |
|------|--------|--------|--------|--------|----------|----------|
| 原始数据 | 37.2 | 37.2 | 2555 | 2501 | 0 | 0 |
| 等待压力 | 30.6 | 30.6 | 2555 | 2501 | 0 | 0 |
| 截止时间压力 | 37.2 | 37.2 | 2555 | 2555 | 2 | 2 |

**分析**：
- 改进策略在 T3 成本上有 2.1% 的降低，说明综合评分策略更有效利用容量和路线
- T2 不满意度在示例数据上两策略相同（所有 S_i=0，区分度有限）
- 截止时间压力场景下出现超时（deadline 设置非常紧），两策略超时数相同

### 5.4 自造数据验证

两组自造数据位于 `测试数据/自造1/` 和 `测试数据/自造2/`：

| 数据集 | T1: 0→3 | T2 不满意度 | T3 总成本(改进) | T3 超时 |
|--------|---------|-------------|----------------|---------|
| 自造1 | 7 | 22.8 | 1539 | 0 |
| 自造2 | 6 | 28.0 | 1739 | 0 |

---

## 6. 选做对比结论

1. **T4**：状态压缩 DP 在退货点 ≤15 时给出精确最优解；>15 点时最近邻启发式保证效率。两种方法自动切换。
2. **T5**：多候选 + 局部优化策略在示例数据上给出双车总成本 2465，比单车 T3 改进策略的 2501 更低，体现了双车协同的优势。
3. **T6**：改进策略在部分场景下降低 T3 成本（自造1 降低 3.4%，自造2 降低 2.9%）。T2 不满意度变化较小，说明等待时间利用策略还有优化空间。
4. **可视化**：完整实现了 Canvas 交互式地图、多视图切换、配送动画、数据面板等功能，可直接在浏览器中查看全部结果。

---

## 7. 总结

本项目已完成全部必做（T1-T3）和选做（T4-T6 + 可视化）内容：

- ✅ 三种自实现数据结构：邻接表、最小堆、循环队列
- ✅ T1：Dijkstra 最短路 + 缓存优化
- ✅ T2：不满意度最小化（基线 + 改进策略对比）
- ✅ T3：运送成本与超时计算（基线 + 改进策略对比）
- ✅ T4：退货回收（DP + 启发式自适应）
- ✅ T5：双车协同配送（多候选 + 局部优化）
- ✅ T6：策略对比实验（3 场景 × 2 策略）
- ✅ 可视化：HTML Canvas 交互式界面
- ✅ AI 使用日志
- ✅ 开发过程记录
- ✅ 中文路径兼容
- ✅ JSON 数据导出
- ✅ Makefile 构建系统
