# AI 使用日志

## 1. 我用 AI 做了什么

本项目在实现与整理过程中使用 AI 主要做了以下事情：

1. 帮助梳理题目要求，确认 T1-T6 的交付范围。
2. 协助检查代码中输入输出路径、命令行参数和 Windows 中文路径兼容问题。
3. 协助补充和优化了若干功能：
   - 最短路缓存
   - T2 / T3 的完整配送方案输出
   - T4 的退货点输入流程
   - T5 的双车协同启发式优化
   - T6 的策略对比实验框架
4. 帮助整理项目报告和实验说明的 Markdown 结构。

## 2. AI 给错或不够好的地方

### 2.1 中文路径处理方案一开始不稳定

最开始的输入处理使用了普通字符串和 `filesystem::path`，在 Windows 中文目录下出现了路径转换错误。后来我发现问题后，改成：

- 使用 `wmain`
- 编译时加 `-municode`
- 通过宽字符路径读取数据目录

这是通过实际运行示例数据后发现并修正的。

### 2.2 一开始的 T4 不是独立输入

AI 最初给出的 T4 是从现有包裹目的地推导退货点，这样虽然能跑，但不符合“单独可配置退货输入流程”的要求。后来我补成了：

- 如果存在 `returns.txt`，就直接读取退货点
- 不存在时再回退到自动推导

这样才满足更明确的输入流程。

### 2.3 一开始的 T5 只是简单启发式

最初的双车方案只是按一个粗略分数把包裹切给两辆车，效果不稳定，也不是很像“优化”。后来我把它改成：

- 多种初始分配方案
- 局部交换与搬移优化
- 从多个候选方案里选更优者

这样结果更稳定，也更符合“协同优化”的味道。

### 2.4 T6 的实验场景需要再解释

AI 生成的对比实验框架虽然能输出数据，但场景定义还比较泛。我后来补充了：

- 原始数据
- 等待压力场景
- 截止时间压力场景

并在报告中说明这些场景分别想验证什么。

## 3. 我如何发现并修正错误

我主要通过以下方式发现问题：

1. 实际编译，查看编译错误。
2. 直接运行示例数据，检查是否能读入中文目录。
3. 对照题目说明逐项核对输出内容是否满足要求。
4. 观察测试输出是否包含完整配送方案，而不仅仅是汇总数值。

发现问题后，我会先缩小到单个文件或单个函数，再做最小修改并重新验证。

## 4. 我最有把握的部分

我最有把握的是：

- 数据读取和命令行入口
- Dijkstra 和最短路缓存
- T2 / T3 的基本模拟流程
- 输出格式整理

这些部分都经过了实际编译和运行验证。

## 5. 我最没把握的部分

我最没把握的是：

- T5 的双车分配是否全局最优
- T4 在大规模退货点情况下的性能上限
- T6 的改进策略是否在所有数据上都显著优于基线

这些部分本质上是启发式或组合优化问题，当前实现偏向“可运行、可比较、可展示”，不保证全局最优。

## 6. 总结

AI 在本项目里主要起到“加速梳理和补全实现”的作用，但最终可用性还是通过我自己反复编译、运行和对照题目要求确认的。对于题目交付而言，关键不是 AI 是否参与，而是结果是否满足输入格式、输出要求和实验说明。
